{
  "name": "@plagiarism-checker/api",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "nodemon src/server.js",
    "build": "node build.js",
    "start": "NODE_ENV=production node dist/server.js",
    "lint": "eslint src --ext .js",
    "test": "jest",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "express": "^4.19.2",
    "cors": "^2.8.5",
    "helmet": "^7.1.0",
    "compression": "^1.7.4",
    "dotenv": "^16.4.5",
    "string-similarity": "^4.0.4",
    "express-rate-limit": "^7.2.0",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.0",
    "esbuild": "^0.20.2",
    "jest": "^29.7.0",
    "@plagiarism-checker/eslint-config": "workspace:*"
  }
}
